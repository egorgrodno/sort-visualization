import { MouseHoldDirective } from './mouse-hold.directive';

describe('MouseHoldDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseHoldDirective();
    expect(directive).toBeTruthy();
  });
});
